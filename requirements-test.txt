# Testing Requirements for Timepoint-Pro
# Install with: pip install -r requirements-test.txt

# Core pytest
pytest>=7.3.1
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# Optional pytest plugins (recommended)
pytest-xdist>=3.3.0  # Parallel execution
pytest-html>=3.2.0   # HTML reports
pytest-timeout>=2.1.0  # Test timeouts

# Missing dependencies that tests require
sqlmodel>=0.0.22
bleach>=6.0.0
hydra-core>=1.3.2

# Development dependencies
python-dotenv>=1.0.0
